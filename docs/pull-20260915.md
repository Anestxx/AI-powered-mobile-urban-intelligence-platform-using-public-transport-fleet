# Pull and gallery handoff — 15 September 2026

Updated main from 47cba0f to 3c42b44 with a fast-forward of origin/main. The incoming
commits add ambulance.pt and spatial_cluster.py. The ambulance checkpoint exposes
class 0 = ambulance; its Git blob matches the remote commit. Existing weights
were unchanged. The model inventory now records all five checkpoints.

The incoming edits to main.py and models.py conflicted with the local connected
implementation. The local video runner, persistent issue model and strict schema
were retained together. The active backend already groups nearby observations by
issue_id; the incoming in-memory clustering helper is present but is not connected
to that workflow. Downloading the ambulance model does not enable ambulance or
accident alert ingestion in the currently pothole-only contract.

All pre-pull source files matched the recovery archive immediately after conflict
resolution (ignoring Git's line-ending normalization). Local changes remain
unstaged. Recovery sources are in ignored docs/before-pull-20260915-211528.zip;
the corresponding autostash babadfc remains available. Runtime databases,
credentials, recordings and checkpoints were not removed or replaced.

The pending photo gallery implementation was also checked: 68 Python tests and
11 frontend service tests passed, and the production frontend built successfully.
The backend was restarted to expose the new /api/evidence endpoint. The connected
browser tool reported no available browser, so browser interaction and visual
layout checks remain pending.

Training has not run: no labeled train/validation/test dataset is present. A
review set was prepared from the bundled recording, with 31 frames and 55
unverified proposals. No proposals were treated as training labels.
